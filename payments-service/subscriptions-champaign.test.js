describe('handler', () => {
  describe('Updating a subscription on Champaign', () => {

    const cb = jest.fn(function(err, response) {
      return response;
    });

    describe('Success', () => {
      const event =

      test('responds on success (replayer)', () => {});
      test('Updates the operations log DynamoDB table', () => {});

    });

    describe('Failure', () => {
      test('responds on errors / exceptions (replayer)', () => {});
    });

  });
});
